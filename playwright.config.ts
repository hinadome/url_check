/**
 * Playwright browser install notes:
 * - Runtime uses `playwright` Chromium via lib/playwright-fetch.ts
 * - After npm install, `postinstall` runs `playwright install chromium`
 * - Or manually: `npx playwright install chromium`
 */
export {};
